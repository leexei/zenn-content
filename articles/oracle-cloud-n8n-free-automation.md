---
title: "Oracle Cloud Free Tier + n8nで完全無料の自動化サーバーを構築してみた"
emoji: "☁️"
type: "tech"
topics: ["n8n", "oraclecloud", "docker", "automation", "free"]
published: true
---

## はじめに

こんにちは！ミミだよ〜✨

今日は **完全無料で自動化サーバーを構築する方法** を紹介するね！

![Oracle Cloud + n8n](/images/oracle-cloud-n8n-free-automation/cover.webp)

「Zapierの無料枠が足りない...」「自動化をもっと自由にやりたい...」そんな悩み、ない？

実は **Oracle Cloud Free Tier** を使えば、**月額$0で無制限のワークフロー自動化サーバー**が手に入るんだ！🎉

## 何を作るの？

```
┌─────────────────────────────────────────────────┐
│ 完成イメージ                                     │
├─────────────────────────────────────────────────┤
│ Oracle Cloud (永久無料)                          │
│   └─ Ubuntu + Docker                            │
│       └─ n8n (ワークフロー自動化)                │
│           ├─ Slack連携                          │
│           ├─ Webhook受信                        │
│           ├─ 定期実行（無制限！）                │
│           └─ etc...                             │
└─────────────────────────────────────────────────┘

💰 月額コスト: $0
🔄 ワークフロー数: 無制限
⚡ 実行回数: 無制限
```

## 自動化ハブの無料枠比較

まず、各サービスの無料枠を比較してみよう：

| サービス | 無料枠 | 制限 |
|----------|--------|------|
| Zapier | 100タスク/月 | 5 Zap まで |
| Make | 1,000オペ/月 | - |
| n8n Cloud | 5ワークフロー | - |
| **n8n (セルフホスト)** | **無制限** | 自分でサーバー用意 |

n8nをセルフホストすれば無制限！でもサーバー代がかかる...🤔

**そこで Oracle Cloud Free Tier！**

## Oracle Cloud Free Tier とは

Oracleが提供する**永久無料**のクラウドサービス：

| リソース | 無料枠 |
|----------|--------|
| ARM (Ampere A1) | 4コア / 24GB RAM |
| AMD x86 | 2台 (各1コア / 1GB RAM) |
| ストレージ | 200GB |
| 転送量 | 10TB/月 |

**AWS/GCPの無料枠との違い：**
- AWS/GCP → 12ヶ月で終了
- Oracle → **永久無料** ✨

n8nを動かすには十分すぎるスペック！

## 構築手順

### Step 1: Oracle Cloud アカウント作成

1. https://www.oracle.com/cloud/free/ にアクセス
2. 「Start for free」をクリック
3. 必要情報を入力（クレカ登録必要、課金はされない）
4. **Home Region** を選択（後から変更不可！）

> 💡 東京・大阪が近いけど、人気リージョンは容量不足になりやすい

### Step 2: インスタンス作成

1. Oracle Cloud Console にログイン
2. ≡ メニュー →「Compute」→「Instances」
3. 「Create instance」

**設定内容：**

| 項目 | 設定値 |
|------|--------|
| Name | `n8n-server` |
| Image | Ubuntu 22.04 |
| Shape | VM.Standard.A1.Flex (ARM) または VM.Standard.E2.1.Micro (AMD) |
| OCPU | 2（ARMの場合） |
| Memory | 12GB（ARMの場合） |

> ⚠️ ARMは人気で「Out of capacity」エラーが出ることも。その場合はAMD x86で代用！

**ネットワーク設定：**
- VCN: 「Create VCN with Internet Connectivity」で新規作成がおすすめ
- Public IPv4: **必ず有効化！**

**SSH Keys：**
- 「Generate a key pair for me」を選択
- **秘密鍵を必ずダウンロード！**（後から再取得不可）

### Step 3: Security List でポート開放

Oracle Cloud Console で：

1. Networking → Virtual Cloud Networks → 作成したVCN
2. Security Lists → Default Security List
3. 「Add Ingress Rules」で以下を追加：

| Source CIDR | Protocol | Port | 用途 |
|-------------|----------|------|------|
| 0.0.0.0/0 | TCP | 22 | SSH |
| 0.0.0.0/0 | TCP | 5678 | n8n |

### Step 4: SSH接続

```bash
# 秘密鍵のパーミッション設定
chmod 600 ~/.ssh/your-key.key

# SSH接続（Ubuntuの場合）
ssh -i ~/.ssh/your-key.key ubuntu@<Public IP>
```

### Step 5: Docker インストール

```bash
# パッケージ更新
sudo apt update && sudo apt install -y docker.io

# Docker有効化
sudo systemctl enable docker
sudo systemctl start docker
sudo usermod -aG docker $USER

# 一度ログアウトして再接続
exit
```

### Step 6: n8n 起動

```bash
# データディレクトリ作成（パーミッション重要！）
mkdir ~/.n8n
sudo chown -R 1000:1000 ~/.n8n

# n8n起動
docker run -d --name n8n \
  --restart always \
  -p 5678:5678 \
  -v ~/.n8n:/home/node/.n8n \
  -e N8N_SECURE_COOKIE=false \
  n8nio/n8n
```

> 💡 `N8N_SECURE_COOKIE=false` はHTTPアクセス用。本番はHTTPS推奨！

### Step 7: OS ファイアウォール設定

Oracle Cloud の Ubuntu はデフォルトで iptables が有効：

```bash
# ポート5678を開放
sudo iptables -I INPUT 6 -m state --state NEW -p tcp --dport 5678 -j ACCEPT

# 設定を永続化
sudo netfilter-persistent save
```

### Step 8: アクセス確認

ブラウザで `http://<Public IP>:5678` にアクセス！

n8n のセットアップ画面が表示されたら成功！🎉

## Slack連携してみる

### Slack App 作成

1. https://api.slack.com/apps で「Create New App」
2. 「From scratch」→ App名とWorkspaceを設定
3. 「OAuth & Permissions」→ Bot Token Scopes に追加：
   - `channels:history`
   - `channels:read`
   - `chat:write`
   - `users:read`
4. 「Install to Workspace」
5. `xoxb-` で始まる Bot Token をコピー

### n8n で設定

1. n8n → Credentials → Add Credential
2. 「Slack API」を選択
3. Access Token に `xoxb-...` を貼り付け

### メッセージ取得ワークフロー

Slack の組み込みノードだと `search` API が Bot Token では使えないので、HTTP Request で直接 API を叩く：

1. HTTP Request ノードを追加
2. 設定：
   - Method: `GET`
   - URL: `https://slack.com/api/conversations.history`
   - Query Parameters: `channel` = チャンネルID, `limit` = 10
   - Headers: `Authorization` = `Bearer xoxb-...`

これでメッセージ取得できる！

## ハマりポイントと解決策

### 1. ARMインスタンスが作れない

```
Out of capacity for shape VM.Standard.A1.Flex
```

**解決策：** AMD x86 (VM.Standard.E2.1.Micro) で代用。1GB RAMでもn8nは動く！

### 2. SSH接続がタイムアウト

**原因：** Security List か Route Table の設定漏れ

**解決策：**
- Security List で Port 22 が開いてるか確認
- Route Table に `0.0.0.0/0 → Internet Gateway` のルールがあるか確認

### 3. n8nコンテナが起動しない

```
EACCES: permission denied, open '/home/node/.n8n/config'
```

**原因：** ボリュームのパーミッション

**解決策：**
```bash
sudo chown -R 1000:1000 ~/.n8n
```

### 4. ブラウザからn8nにアクセスできない

**原因：** Ubuntu の iptables

**解決策：**
```bash
sudo iptables -I INPUT 6 -m state --state NEW -p tcp --dport 5678 -j ACCEPT
```

## 活用アイデア

n8nが無制限で使えるようになったら、こんなことができる：

| ユースケース | 説明 |
|-------------|------|
| Webhook受信 | GitHub、Stripe等のイベントを受信 |
| 定期実行 | 毎分チェック系のタスク（Zapierだとクレジット消費激しい） |
| データ変換 | 重い処理も気兼ねなく実行 |
| Slack監視 | 新着メッセージを別チャネルに通知 |
| RSS監視 | ブログ更新チェック |

## まとめ

Oracle Cloud Free Tier + n8n で、**完全無料の自動化サーバー**が構築できた！😊

- 💰 月額: **$0**
- 🔄 ワークフロー: **無制限**
- ⚡ 実行回数: **無制限**
- 🌐 24時間稼働

Zapierの無料枠が足りない人、自動化をもっと自由にやりたい人は、ぜひ試してみてね！

---

**ミミより** 💕

## 参考リンク

- [Oracle Cloud Free Tier](https://www.oracle.com/cloud/free/)
- [n8n公式ドキュメント](https://docs.n8n.io/)
- [n8n Docker Setup](https://docs.n8n.io/hosting/installation/docker/)
